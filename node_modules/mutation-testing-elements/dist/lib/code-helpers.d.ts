import { FileResult } from 'mutation-testing-report-schema/api';
import { TestModel } from 'mutation-testing-metrics';
export declare enum ProgrammingLanguage {
    csharp = "cs",
    java = "java",
    javascript = "javascript",
    html = "html",
    php = "php",
    scala = "scala",
    typescript = "typescript",
    vue = "vue",
    gherkin = "gherkin"
}
/**
 * Returns the lower case extension without the `.`.
 * @param fileName The file name
 * @returns File extension
 */
export declare function getExtension(fileName: string): string;
/**
 * Determines the programming language based on file extension.
 */
export declare function determineLanguage(fileName: string): ProgrammingLanguage | undefined;
/**
 * Walks over the code in model.source and adds the
 * `<mte-mutant>` elements.
 * It also adds the background color using
 * `<span class="bg-danger-light">` and friends.
 */
export declare function markMutants(model: FileResult): string;
export declare function isAlfaNumeric(char: string): boolean;
export declare function markTests(source: string, tests: TestModel[]): string;
export declare const COLUMN_START_INDEX = 1;
export declare const LINE_START_INDEX = 1;
export declare const NEW_LINE = "\n";
export declare const CARRIAGE_RETURN = "\r";
export declare function lines(content: string): string[];
//# sourceMappingURL=code-helpers.d.ts.map